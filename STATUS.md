# 현재 상태 — 세션 인수인계 (2026-08-26)

새 세션은 이 파일부터 읽는다. 그다음 `CLAUDE.md`(프로젝트 지침), `SPEC.md`(기능 명세),
경쟁 구도가 필요하면 `COMPETITORS.md`(2026-08-26 조사 — 경쟁사가 맛집을 찾아내는 방식과 우리 틈).

- **역할 구분**: CLAUDE.md = 규칙 / **STATUS.md = 지금 어디까지 왔나(이 문서)** / SPEC.md = 무엇이 구현돼 있나
- 배포: `vercel --prod` 또는 **main 푸시 → Vercel 자동 빌드**(GitHub 연동돼 있음)
- 실서비스: https://meogja.vercel.app

---

## 이번 작업 (2026-08-26) — 주변 맛집 지도 보기

owner 요청: "주변식당 찾고 나서 결과를 맵으로 해서 현재위치랑 주변에서 어디에 있는지 보여줄 수 있어?"

### 만든 것
- `src/components/NearbyMap.tsx` (신규) — 카카오 지도 JS SDK 래퍼
- `src/app/nearby/page.tsx` — 정렬 탭 옆에 **목록 / 지도** 토글, 선택한 가게 카드
- `scripts/check-nearby-map.mjs` (신규) — 헤드리스 확인 스크립트

동작: 내 위치는 파란 점(pulse), 식당은 카테고리 이모지 마커. 마커를 누르면 그 마커가
주황으로 커지고 지도 아래에 가게 카드가 붙는다(카카오맵·네이버맵·구글맵 링크 +
"같이 먹을 사람 구하기"). 전체가 한 화면에 들어오도록 `setBounds` 로 줌을 맞춘다.

**데이터는 새로 받은 것이 없다.** `/api/nearby` 가 이미 카카오 FD6 검색의
`mapx`(경도)·`mapy`(위도)를 내려주고 있었고, 화면도 기준 좌표를 `coords` 로 들고 있었다.

### 커밋
| 커밋 | 내용 |
|------|------|
| `5edae4e` | 지도 기능 본체 (NearbyMap + 토글 + 카드) |
| `80440a1` | 마커를 고를 때마다 화면이 원위치로 튕기던 것 |
| `29fed19` | 실물 확인에서 드러난 셋 — 깨진 아이콘·잘린 이름표·안 보이는 카드 |

### 곁다리로 고친 원래 버그
`nearby/page.tsx` 의 `FOOD_ICONS` 가 `/food-icons/korean.png` 같은 **없는 영문 경로**를
가리키고 있었다. 실제 아이콘 140개는 전부 한글 파일명이고 매핑은 `lib/foodIcons.ts` 의
`getFoodIconUrl` 이 이미 갖고 있었다. 사진 API 가 빈손일 때마다 **목록 카드도 같이
깨지고 있었다** — 지도 카드를 만들다 발견해 양쪽을 함께 고쳤다(404 6건 소멸).

### 확인
- `npx next build` 통과 / `tsc --noEmit` 통과 / eslint 신규 error 0
- 로컬 헤드리스 통과, **실서비스 통과**(SDK 로드·타일·마커 탭 → 카드, 콘솔 에러 0)
- 배포본 번들에 JS 키가 실제로 박힌 것까지 파일로 확인

---

## 이어서 (2026-08-26) — 지도를 기본으로, 홈 진입 화면에도 지도

owner 요청: "지도를 기본값으로. 그리고 주변찾기 탭 말고 홈에서 주변식당찾기 눌러 들어갔을
때는 지도가 안 뜨는데 그때도 뜨게."

- `/nearby` 기본 뷰: 목록 → **지도**
- 홈의 "📍 모임 없이 바로 주변 찾기" 는 `/search` 로 간다 — **이 화면엔 지도가 아예 없었다.**
  같은 토글 + 지도를 붙였다(기본도 지도).
- 지도 + 가게 카드를 `src/components/MapPanel.tsx` 로 뽑았다. 카테고리·거리·지도링크
  헬퍼는 `src/lib/foodCategory.ts` 로 모았다 — 카드 복사본이 갈라져 아이콘 버그를 한쪽만
  고치는 일이 있었다(어제 그 일이 실제로 있었다).
- 🔴 **제공자별 좌표 형식이 다르다.** 네이버(`/api/search`)는 mapx/mapy 를 **1e7 배 정수**로
  내려준다(그 라우트는 거리 계산에만 나눠 쓰고 응답은 원본을 넘긴다). 카카오·구글은 십진 도.
  안 맞추면 마커가 지구 밖으로 간다 → `normalizeCoord` 가 흡수. 좌표 없는 결과는 지도에서
  빼고 문구로 알린다.
- 지도가 안 뜨면(키 없음·도메인 미등록·SDK 실패) `NearbyMap` 이 `onUnavailable` 로 알리고
  화면이 **자동으로 한 번 목록으로 되돌아간다.** 지도가 기본이 된 순간 이 대비가 없으면
  실패 시 결과가 통째로 안 보인다. 사용자가 다시 '지도'를 누르면 그대로 둔다.

확인: `tsc --noEmit` 통과 / `next build` 통과 / eslint 신규 error 0(기존 2건만 남음)
헤드리스: `node scripts/check-nearby-map.mjs`, `node scripts/check-search-map.mjs` 둘 다 통과
  - ⚠️ **포트 3000 으로 확인해야 한다.** 카카오 콘솔에 등록된 로컬 도메인이 `localhost:3000`
    뿐이다. 3311 등 다른 포트로 띄우면 SDK 가 401 을 주고 '지도를 불러오지 못했어요' 가 뜬다.
  - `/nearby` 목록 카드는 주소를 적지 않는다 — 목록 전환 판정에 주소 문구를 쓰면 안 된다.

알아두면 좋은 것: `/search` 는 `meogja_preset_menus` (고른 메뉴)가 없으면 검색을 아예 안 한다.
홈에서 메뉴를 안 고르고 들어오는 경로가 있으면 결과가 0건이라 지도도 비어 있다 — 이건 지도
문제가 아니다.

---

## 운영 설정 — 지도가 뜨려면 둘 다 필요하다

한쪽만 있으면 지도는 안 뜬다(앱은 안 깨진다. 안내 화면이 대신 뜨고 목록은 그대로 쓴다).

1. **카카오 콘솔의 JavaScript SDK 도메인** — 등록 완료
   `developers.kakao.com` → 내 애플리케이션 → 오늘뭐먹지?(meogja) → 앱 설정 > 앱 >
   **플랫폼 키** → JavaScript 키 [Default JS Key] 수정 → **JavaScript SDK 도메인**
   - 등록된 값: `https://meogja.vercel.app`, `http://localhost:3000`
   - ⚠️ 콘솔이 개편돼 '플랫폼' 메뉴가 없다. '일반' 페이지에도 없다. **플랫폼 키** 안이다.
   - 미등록이면 SDK 가 401 `domain mismatched` 를 준다. 이 한 줄로 원인이 바로 갈린다:
     `curl -s -H "Referer: https://meogja.vercel.app/" "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<JS키>&autoload=false" | head -c 120`
   - 새 도메인(커스텀 도메인 등)을 붙이면 여기에도 반드시 추가해야 한다.

2. **Vercel 환경변수 `NEXT_PUBLIC_KAKAO_JS_KEY`** — 등록 완료
   - ⚠️ **빌드 시점에 앱 안으로 박히는 값이다.** 값을 바꾸면 **재배포해야** 반영된다.
   - Secret 으로 등록돼 있어도 빌드는 읽는다(현재 그렇게 돼 있다). 다만 값을 다시 볼 수
     없다 — `NEXT_PUBLIC_` 값은 어차피 브라우저에 공개되므로 Config 가 더 편하다.
   - 배포본에 제대로 박혔는지 확인하는 법: 실서비스 JS 번들을 받아 키 문자열을 찾는다.

### 돈 드는 설정은 필요 없다
- 카카오맵은 **제품 설정 > 카카오맵 → 사용 설정 ON** 이면 되고, 이 앱에는 **무료 쿼터**가
  붙어 있다(앱 이름 옆 '카카오맵 무료 쿼터' 뱃지).
- **유료 API 화면의 '카카오맵' 은 '사용 안 함' 으로 두어야 한다.** '사용 함' 으로 바꾸면
  그때부터 사용 금액이 청구된다. 지도 표시에는 필요 없다.
- JS 키 수정 화면의 '카카오 로그인 리다이렉트 URI' 는 **비워 둔다.** 먹자냥의 카카오
  로그인은 Supabase 를 거치고, 그 주소는 제품 설정 > 카카오 로그인 쪽(REST 키)에 이미
  등록돼 있다. 여기에 넣으면 설정이 두 군데로 갈라진다.

---

## 로컬 개발에서 알아둘 것

- **로컬에는 실제 키가 없다.** Supabase·카카오 REST 키는 Vercel 환경변수에만 있어서
  `npm run dev` 로는 검색 API 가 500 을 낸다. 화면 확인은 `scripts/check-nearby-map.mjs`
  처럼 API 응답을 가짜로 끼워 넣어서 한다.
- 🔴 **dev 서버를 띄운 채 `next build` 를 돌리지 마라.** 빌드가 같은 `.next` 를 덮어써서
  돌던 dev 서버가 깨진다(증상: 멀쩡하던 화면이 갑자기 API 500/ERR_FAILED). 빌드는 dev 를
  멈추고 하거나, 빌드 뒤 dev 를 다시 띄운다.
- 🔴 **Playwright 로 이 앱을 확인할 때는 `serviceWorkers: "block"` 을 켜라.** PWA 라
  서비스워커가 요청을 가로채면 `page.route` 모킹이 통째로 무시된다(같은 스크립트가
  어떤 실행에서는 되고 어떤 실행에서는 안 되는 형태로 나타난다). 모킹은 `page.route`
  보다 `context.route` 가 안전하다.

---

## 텔레그램 봇 (이 저장소 밖)

- 실행: 저장소 밖 `/home/user/dev/tmp/run_telegrambot.sh meogja`
- meogja 전용 봇 토큰은 `meogja/.token` (권한 600, `.gitignore` 44행으로 커밋 제외)
- 봇: `@desktop_empty_1_bot`
- 프로젝트마다 토큰·STATE_DIR·tmux 세션 이름·작업 폴더가 갈려 있어 다른 봇을 죽이지 않는다.
- ⚠️ 런처 스크립트 자체는 `/home/user/dev/tmp` 에 있고 **그 폴더는 git 저장소가 아니다.**
  버전관리 밖이라 백업본(`run_telegrambot.sh.bak-*`)으로만 이력이 남는다.

### 시스템 프롬프트는 이제 프로젝트별로 렌더된다 (2026-08-26)

`system_prompt.txt` 는 **템플릿**이고, 런처가 `{{PROJECT}} {{TOKEN_FILE}} {{STATE_DIR}}
{{BOT_USERNAME}}` 를 채워 `$STATE_DIR/system_prompt.rendered.txt` 로 렌더한 뒤 claude 에
넘긴다(토큰 **값**은 렌더하지 않는다 — 경로만).

고친 문제: 공용 프롬프트에 tmp 봇의 토큰 경로와 chat_id 가 하드코딩돼 있어서, meogja 봇
세션이 그 지시대로 curl 답장을 하면 **엉뚱한 봇(`@dev183_tmp_bot`) 창으로 갔다.**

같이 바로잡은 것 — 🔴 **`getUpdates` 를 호출하지 않는다.** 토큰당 폴러가 1개뿐이라
채널 서버가 쓰는 자리를 빼앗아 **내 채널이 끊기거나 사용자 메시지를 잃는다.** 사용자
답장은 자동으로 `<channel>` 메시지로 들어오므로 질문을 보낸 뒤 턴을 끝내면 된다.
답장은 MCP reply 툴이 기본, curl 은 `sendMessage` 전용 예비 수단(이건 폴러와 무충돌).

---

## 다음에 할 일

지도 관련(작은 것부터):
- [x] 아이콘 매핑 구멍 메움 (2026-08-26) — `술집`·`패스트푸드` 외에 카카오 잎 카테고리
      (호프,요리주점 / 일본식주점 / 도시락 / 아시아음식 …)까지. `scripts/check-food-icons.mjs`
      가 매핑 + **파일 존재**까지 확인한다(예전에 없는 경로를 가리켜 404 가 났다)
- [x] 목록 ↔ 지도에서 보던 가게 유지 (2026-08-26) — 목록 카드에 "🗺️ 지도에서" 버튼.
      누르면 그 가게가 골라진 채로 지도가 열린다(좌표 없는 결과에는 안 붙는다)
- [x] "이 지역에서 다시 찾기" (2026-08-26) — 지도를 400m 넘게 밀면 지도 위에 버튼이 뜨고,
      누르면 그 자리로 재검색 + 헤더 동네 이름도 역지오코딩으로 바꾼다.
      밀었던 기준점을 함께 들고 있어(baseKey) 재검색 후 옛 권유가 저절로 무효가 된다
- [ ] 마커가 겹칠 때 뭉치기(클러스터) — 지금은 15개라 견디지만 반경을 넓히면 이름표가 엉킨다

- [x] 마커 뭉치기(클러스터) (2026-08-26) — 겹침은 **화면 픽셀** 문제라(줌마다 달라진다)
      화면 좌표로 바꿔 **26px** 안쪽끼리 묶는다(62 → 36 → 26px 로 두 번 좁혔다.
      실물에서 "너무 합쳐져 안 보인다" 는 말이 두 번 나왔다 — 마커 동그라미가 38px 이니
      거의 같은 자리일 때만 묶는다).
      묶어도 **가게 이름을 보여준다**(두 곳까지 적고 나머지는 `+N곳`) — 이름이 없으면
      누르기 전에는 무엇이 묶였는지 알 수 없다.
      펼치기는 **레벨 2 이하**에서 바로 된다(레벨 1 만 보던 때는 2 에서 몇 번 눌러도 반응이 없었다).
      🔴 펼친 상태는 **가게 번호로** 기억한다(묶음 단위로 기억하면 안 된다). 줌이 바뀌면
      묶이는 조합 자체가 달라져서, 다른 자리를 펼치는 순간 앞서 펼친 것이 다시 묶였다.
      접는 시점도 줌이 아니라 **밀었을 때·결과가 새로 왔을 때** 로 잡는다 — 다른 자리를
      펼치려고 누르면 지도가 확대되는데, 그것까지 세면 방금 펼친 것이 접힌다.
      (owner 신고: "하나를 펼치면 다른 게 접힌다")

### 같은 가게가 여러 번 나오던 것 (2026-08-26)

`src/lib/dedupePlaces.ts` — 이름을 다듬고(태그·괄호·지점 접미사 제거) **좌표가 80m 안**
이면 같은 가게로 본다. 좌표가 없으면 주소 앞부분으로 견준다. 적용: `/nearby`, `/search`,
모임 화면 식당 검색.

왜 생겼나:
1. `/search` 와 모임 화면은 **고른 메뉴마다 따로 검색해 합친다.** 한 집이 두 메뉴에
   걸리면 두 번 들어온다. 예전 판별 기준이 `이름|주소` 완전일치라, 같은 집인데 주소가
   도로명/지번으로 갈리면 다른 집으로 봤다.
2. 검색 제공자의 등록 자체가 갈려 있다("○○식당", "○○식당 본점", "○○식당 1호점").
   ⚠️ **여러 제공자를 섞어서 그런 것은 아니다** — 한 번 검색은 한 제공자만 쓴다
   (`/api/nearby` 는 카카오 FD6 하나, `/search` 는 설정된 제공자 하나).

이름만 보면 진짜 다른 지점(강남점/역삼점)을 지우고, 좌표만 보면 한 건물의 다른 가게를
지운다 — 그래서 둘을 함께 본다. 확인: `node scripts/check-dedupe.mjs` 고른 가게는 절대 묶지 않는다.
      누르면 그 안으로 확대, **최대 확대인데도 붙어 있으면 동그랗게 펼친다**
      (같은 건물에 여러 곳이면 확대로는 영원히 안 갈라진다 — 유일한 출구).
      🔴 `coordsFromContainerPoint` 에는 **`new kakao.maps.Point(x,y)` 를 넘겨야 한다.**
      평범한 `{x,y}` 를 주면 SDK 안에서 `b.e is not a function` 으로 터진다(실측).
      🔴 줌 변화는 부모에 "옮겼다" 고 알리지 않는다 — 클러스터를 눌러 확대한 것까지
      이동으로 세면 "이 지역에서 다시 찾기" 가 계속 뜬다.
      확인: `node scripts/check-map-cluster.mjs`

## 선호도 → 메뉴 결정 로직 손보기 (owner 요청, 2026-08-26 접수 · 아직 안 함)

owner: "선호도 조사에서 **못 먹는 음식**을 제대로 파악하고 **공통으로 좋아하는 것**을 찾아
맞춘 메뉴를 주변에서 찾는 로직을 강화해야겠다. 지금 제대로 되는지도 모르겠고, 선호도대로
**너무 같은 결과만** 나오는 것도 별로다 — 상수값에 랜덤을 조금 섞어야 할 수도. 그리고 선호도
조사가 하나하나 보기 귀찮다 — 차라리 **최초 로그인 시 튜토리얼로 선호도 조사**를 들어가게 하고
더 재미있게 만드는 것도 방법."

### 미리보기 이미지 최종안 — 먹자냥 말풍선 (2026-08-26)

owner 가 시안 4개 중 **말풍선** 을 골랐다. 나머지 시안 코드는 지웠다.

- 왼쪽 아래에 **채팅 꼬리**. 🔴 satori 에서 삼각형은 border 트릭이 잘 안 먹는다 →
  **정사각형을 45도 돌려** 쓴다. 튀어나오는 정도는 `left` 음수값이 정한다
  (작으면 풍선에 가려져 혹처럼 보인다 — -16 → -38 로 키웠다).
- 글꼴: Noto Sans KR 800/500. 🔴 **글꼴을 안 넘기면 기본 글꼴로 그려져 한글이 얇다.**
  전체 한글 글꼴은 수 MB 라 `text=` 부분집합으로 받는다(구형 UA → ttf. satori 는 woff2 못 읽음).
- 크기 1200x630 → **1080x630**(owner 요청: 가로를 조금 줄임)
- 문구: 아래 "먹자냥이 취향을 모아 정했어요" / 위는 모임이면 "○○의 선택",
  혼자 뽑은 것이면 "오늘은 이거 어때요?"

### 카톡 미리보기 이미지가 못생겼던 이유 (2026-08-26)

owner: "카톡 공유할 때 썸네일이 왜 안 예쁘냐."

원인 두 개였다.
- 🔴 **이모지를 그리고 있었다.** `next/og`(satori)에는 **색 이모지 글꼴이 없다.** 그래서 🍽️ 가
  흐릿한 회색 접시로 나왔다(실물 캡처로 확인). → 이모지를 쓰지 않고, 우리가 가진 **음식 그림
  PNG 140장**을 쓴다. 메뉴 이름을 `getFoodIconUrl` 로 찾아 그 그림을 넣고, 없으면 마스코트로.
- 🔴 **옅은 주황 배경에 흰 글씨**여서 대비가 낮아 글자가 날아갔다. → 흰 카드를 깔고 진한
  글씨를 얹었다. 제목 크기는 글자 수에 따라 줄여 한 줄에 담는다(두 줄이면 카드가 무너진다).

확인: `scripts/check-result-share.mjs` 가 **파일 크기**도 본다(40KB 미만이면 그림이 빠진 것으로
간주). 이모지만 그리던 예전 이미지는 훨씬 작았다.

⚠️ 카카오는 미리보기를 **최대 24시간 캐시**한다. 예전에 공유한 링크는 옛 그림이 보일 수 있다.
`/api/kakao-clear?url=...` 로 캐시를 털 수 있다(이미 있는 라우트).

### 홈 랜덤: 식사 / 디저트 토글 (2026-08-26)

owner: "디저트로 찾기 링크는 안 어울린다. 버튼(토글)로 하고, 디저트로 뽑으면 새로고침도
디저트가 나와야 한다. 제목도 '디저트 뭐먹지?' 로."

- `dessertRoulette`(한 번만 적용되는 값) → `rouletteMode: "meal" | "dessert"` **모드**로 바꿨다.
  다시 뽑기가 그 모드를 그대로 쓴다. 모드를 바꾸면 이전 결과를 지운다.
- 🔴 결과가 나오면 뽑기 버튼이 **아이콘만** 남는다 → `aria-label` 을 넣었다. 이름이 없으면
  스크린리더도, 확인 스크립트도 그 버튼을 못 찾는다(실제로 스크립트가 못 눌러서 "결과가
  안 바뀌는데도 통과" 하고 있었다).
- 🔴 결과 문구에 `data-testid="roulette-result"` 를 붙였다. 텍스트로 찾으면 엉뚱한 문구
  ("배고파? 같이 정하자!")를 집어 검사가 헛돈다 — 실제로 그렇게 헛돌았다.
- 확인: `node scripts/check-home-roulette.mjs`

### 광고에 오염되지 않는 신호로 순위를 만든다 (2026-08-26)

owner 와 정리한 방향: 인기(리뷰·블로그·SNS)는 **전부 광고가 개입하는 축**이고, 그 축에서는
리뷰 수천만 건을 가진 곳을 이길 수 없다. 인스타는 API 제약(비즈니스 계정·주 30개 해시태그)으로
사실상 불가, 블로그 언급량은 협찬이 섞여 owner 가 보류. → **"이 모임에 맞는가" 로 축을 옮겼다.**

① `src/lib/fitScore.ts` — 모임원 중 몇 명의 좋아함이 이 가게와 맞나 → "🎯 우리 취향 82%"
   + 맞는 사람 이름. "우리 취향순" 정렬도 추가.
   🔴 맞는 사람이 없으면 **숫자를 내놓지 않는다**(null). 0% 는 가게를 깎아내리는 말이 되고
   근거도 없다. 🔴 한 글자 겹침은 인정하지 않는다 — 그러면 아무 가게나 100% 가 된다.
   확인: `node scripts/check-fit.mjs`

② 우리 모임 방문 이력 — 먹자팟에 적힌 가게 이름을 세어 "🍚 우리 모임이 N번 갔어요".
   이름 정규화는 중복 제거 로직(`normalizeStoreName`)을 그대로 쓴다(본점/지점 표기 흡수).

③ `src/lib/density.ts` — 후보들끼리의 밀집도로 "🍜 이 근처에 후보 N곳 더". 추가 API 도
   사용자 데이터도 필요 없고 조작도 불가능하다. 여럿이 갈 때 첫 집이 만석일 수 있으니
   실제로 쓸모가 있다. 🔴 **"이 동네 음식점 수" 가 아니라 "지금 후보 중"** 이다 — 문구를
   그렇게 써야 거짓말이 안 된다.

④ 폐업·휴업 표시(LOCALDATA 무료 키 필요)는 owner 판단으로 보류.

### 먹자팟 메뉴 이름 맞추기 (2026-08-26)

owner: "먹자팟에서 메뉴를 임의로 적을 수 있어서 애매하다."

문제: `pat.menu` 가 세 곳에 쓰인다 — 아이콘 찾기, 취향 점수 적립, "이 메뉴 팟 N개" 집계.
자유 입력이면 "삼겹 / 삼겹살 / 매운 삼겹살" 이 서로 다른 메뉴가 되고 오타가 취향 테이블에
쌓인다.

- `src/lib/menuMatch.ts` — 적는 동안 후보를 보여주고(고르면 표준 이름), 저장할 때 가까운
  표준 이름으로 맞춘다. 사용자가 쓴 원문은 **제목에 남는다**.
- 🔴 **사전에 없는 이름으로는 취향 점수를 쌓지 않는다.** 없는 말로 점수를 만들면 추천이
  그 말에 끌려간다. 대신 "알려주셔서 고마워요" 로 끝낸다.
- 맞추는 규칙은 **좁게** 잡았다: 공백만 다른 경우 / 입력이 우리 이름을 포함하는 경우 /
  두 글자 이상이고 앞부분이 일치하는 후보가 **하나뿐**일 때. 넓게 잡으면 엉뚱한 데 붙는다.
- 자유 입력은 계속 허용한다(막으면 답답하고, 사전에 없는 메뉴도 실제로 있다).
  대신 그런 이름이면 "취향 반영·아이콘은 목록에 있는 이름일 때 동작해요" 라고 알려 준다.
- 확인: `node scripts/check-menu-match.mjs`

### 모임 맛집 결과에도 지도 (2026-08-26)

- 모임 화면 식당 결과에 목록/지도 토글. 🔴 **여기만 기본값이 목록이다** — 여러 명이 카드를
  훑어보며 고르는 자리라 지도부터 보여주면 정보가 줄어든다(/nearby·/search 는 지도가 기본).
- 좌표는 `normalizeCoord` 로 맞추고(네이버 1e7), 좌표 없는 결과는 지도에서 뺀다.
  지도를 못 띄우면 목록으로 되돌린다. 새 검색이 오면 고른 가게를 지운다(두 경로 모두).
- ⚠️ **헤드리스 확인이 없다.** 모임 화면은 로그인 + 모임 데이터가 있어야 열리는데 로컬에는
  키가 없다. tsc·build 와 눈으로만 봤다 — Supabase 응답을 모킹하는 확인 스크립트가 필요하면
  `check-taste.mjs`(가짜 세션 + REST 모킹) 방식을 그대로 쓸 수 있다.

### 경쟁사 대비 개선 ②③⑤⑥ (2026-08-26)

② **"또 갈래?" 를 같이 먹은 사람들에게** — 예전에는 팟을 닫는 사람에게만 물어서 신호의
대부분이 버려졌다. 닫힌 팟 카드에 버튼(참여자에게만, 답하면 사라짐) + 닫을 때 참여자에게
알림(`?rate=<patId>` 로 들어오면 바로 물어본다). 답한 팟은 기기에 기억한다.
🔴 물어보는 것을 effect 안에서 바로 부르면 렌더 연쇄가 생긴다 → `setTimeout(…, 0)` 으로 미룬다.

③ **우리 신호를 지도 카드에도** — 먹자팟 수 / 눌러본 횟수. 목록 카드에는 있었는데 지도
카드에는 없었다. 🔴 두 화면이 **같은 기준**(정규화한 이름)으로 세야 다른 말을 하지 않는다.

⑤ **전화·예약 딥링크** — `tel:` + 네이버 예약 검색. 예약 인프라를 만들 이유는 없다.
"정했는데 그다음이 없는" 끊김만 막는다. 전화번호는 검색 결과에 이미 있었고 안 쓰고 있었다.

⑥ **곧 시작하는 먹자팟 띠** (`src/app/PatReminder.tsx`, 홈 상단)
🔴 **서버 크론을 쓰지 않는다.** Vercel Hobby 크론은 하루 한 번이라 "한 시간 뒤 약속" 을 제때
알릴 수 없다. 그래서 앱을 열 때 확인해 띠를 띄운다(알림 권한이 없는 사람에게도 보인다).
🔴 렌더 중에 `Date.now()` 를 부르면 안 된다(값이 흔들린다) → 남은 시간을 찾은 시점에 담아 둔다.
닫으면 그 세션 동안 다시 안 뜬다.

④ 공공데이터(위생등급·모범음식점) — **owner 가 data.go.kr 에서 인증키를 발급해야** 시작할 수
있다. 발급 → Vercel 환경변수 등록 후 진행.

### 경쟁사 대비 개선 ① 결과 카드 공유 (2026-08-26)

우리는 맛집을 **찾아주는** 서비스가 아니라 **정해주는** 서비스인데, 정해진 결과가 밖으로
나가지 않으면 아무도 우리를 모른다(COMPETITORS.md 의 첫 항목).

- `src/app/result/page.tsx`(서버) + `ResultCard.tsx`(클라이언트), `src/lib/shareResult.ts`
- `/api/og?type=result&title=메뉴&sub=모임 또는 사람들` — 미리보기 이미지
- 🔴 **서버에 저장하지 않는다.** 주소에 담는다(`m`, `g`, `who`) — 표도, 지울 데이터도, 만료도 없다.
- "누가 골랐나" 를 이름 칩으로 보여준다 — 받은 사람이 눌러보는 이유가 거기서 나온다.
- 🔴 공유 시트를 **취소한 경우도 예외로 들어온다.** 그때 링크 복사로 떨어지면 "취소했는데
  복사됐다" 가 되므로 따로 처리했다.
- 확인: `node scripts/check-result-share.mjs`

### 알아둘 것 — 홈 하이드레이션 경고 (원인 추정, 안 고침)

실서비스 홈에서 React #418(텍스트 불일치)이 뜬다. 로컬에서는 안 뜬다. 홈이 **시간대에 따라
다른 문구**를 그리는데 서버는 UTC, 브라우저는 KST 라 시간대가 갈리면 문장이 달라지는 것으로
보인다. 화면은 정상이고 메뉴 정리 작업과는 무관하다. 고치려면 시간대 의존 문구를 마운트
후에 정하면 된다(`LoadingCat` 이 쓰는 방식).

### 메뉴 목록 청소 + 랜덤 분리 (2026-08-26)

owner: "없는 메뉴 정리해줘(냉국수·청양떡볶이·치즈치킨). 메인 랜덤에 디저트 안 나오게,
카테고리 랜덤은 따로."

- 조합으로 만든 없는 이름만 제거: **청양떡볶이·치즈치킨·매운찜닭·매운낙지볶음·냉국수**
- 🔴 **브랜드·가게 이름은 빼지 않는다.** 뿌링클·황금올리브·교촌치킨·엽기떡볶이·불닭·
  맥도날드·롯데리아·버거킹·서브웨이는 owner 가 **일부러 넣은 것**이다("오늘 맥도날드" 처럼
  그 이름으로 정한다). 한 번 뺐다가 되돌렸다 — 다시 빼지 마라.
- 🔴 `매운족발`·`매운돼지갈비` 는 **따로 파는 메뉴다**(owner 확인). 이것도 되돌렸다.
  `매운갈비찜`·`불짬뽕` 도 같이 되살렸다.
- `사이드` 카테고리 신설(감자튀김·팝콘·군고구마·나초…) — 치킨에 섞여 있어 한 끼 랜덤에 나왔다
- `isMealFood()` (menus.ts) — 🔴 **시간대·날씨·트렌드 목록에 디저트와 음료가 섞여 있다**
  (오후 = 버블티·빙수·마카롱…). 그대로 랜덤·순위에 넣으면 점심에 마카롱이 나온다.
  홈 랜덤과 홈 추천 순위 양쪽에서 이 함수로 걸러낸다.
- 디저트 랜덤은 히어로 카드의 "🍰 디저트로 뽑기" 로 따로 돈다(`spinRoulette(true)`)
- 확인: `node scripts/check-menus.mjs` — 금지어·디저트 혼입·사이드 혼입·카테고리 이름 혼입

### 3단계 완료 (2026-08-26) — 행동으로 배우기

`src/lib/behaviorScore.ts` — 가중치를 한곳에 모아 뒀다(흩어지면 균형을 못 잡는다).
- 이 메뉴로 식당 찾기 **+1** (`groups/[id]` handleRestaurantByMenus)
- 먹자팟 참여 **+2** (`PatTab.joinPat`)
- 먹자팟 닫을 때 "또 갈래요?" 그렇다 **+3** (`PatTab.closePat`)

쌓이는 곳은 `user_food_scores` 이고 **홈 추천이 이미 이 점수를 읽는다**(page.tsx 의
`homeUserScores`). 월드컵도 같은 테이블에 쌓는다(승리 +1, 결승 +2).

점수가 부풀지 않게: "찾아봤다"(+1)는 **세션에서 메뉴마다 한 번만** 쌓는다. 다시 찾기를
누를 때마다 올라가면 한 메뉴가 다른 모든 신호를 덮는다(홈은 최고점으로 정규화한다).

남은 일 / 알아둘 것:
- [ ] "또 갈래?" 는 **팟을 닫는 사람에게만** 묻는다 — 실제로 같이 먹은 참여자들에게는 안 묻는다.
      가장 크게 남아 있는 신호다(닫을 때 참여자에게 알림을 보내는 식으로 이어갈 수 있다)
- [ ] `ingredientMap.merge()` 에 테스트가 없다
- [x] 튜토리얼이 매번 다시 뜨는 문제 — 아무것도 안 고르고 지나가면 저장이 0건이라
      `hasAnyPreference` 가 계속 false 였다 → `localStorage` 플래그(`meogja_taste_done`)를 같이 본다

🔴 2인자 RPC(`p_food_name`, `p_delta`)를 쓴다 — 함수 안에서 `auth.uid()` 로 자기 것만
올린다. 3인자 형태는 `p_user_id` 를 받는데, 이번에 본인 확인을 넣었다(그전에는 남의
점수를 바꿀 수 있었다). 게스트는 계정이 없어 조용히 넘어간다.

### 2단계 완료 (2026-08-26) — 취향 스와이프 `/taste`

- `src/app/taste/page.tsx` — 3갈래 스와이프(👍/🤷/🚫, 메뉴 단계엔 ⭐최고).
  적응형: 중분류 19장 → 좋다고 한 분류에서 메뉴 16장 → 재료 20종 → 끝 화면
- `src/lib/tastePrefs.ts` — 한 장마다 바로 저장. `score`·`kind` 컬럼이 없는 환경이면
  옛 형태로 한 번 더 시도한다(점수는 못 남기지만 좋아함/못먹음은 남는다)
- 최초 로그인 후 선호도가 하나도 없으면 `/taste?onboarding=1` 로 보낸다(auth/callback).
  프로필 선호도 칸 맨 위에도 입구를 뒀다
- 메뉴 카드에서 재료 제보(`report_menu_ingredient`) — 3명 모이면 반영
- 확인: `node scripts/check-taste.mjs` (가짜 세션 + Supabase 전부 모킹)
  · 🔴 **Playwright 는 나중에 등록한 route 가 먼저 잡는다.** 세는 코드를 catch-all 앞에
    따로 두면 가려져서 "저장 0건" 으로 보인다(실제로 그렇게 헤맸다)
  · 🔴 카드 화면에 `minHeight: 100vh` 를 쓰면 헤더·탭바 때문에 **버튼이 화면 밖으로 밀린다**

### 1단계 완료 (2026-08-26) — 점수·재료·추천 로직

- `src/lib/ingredients.ts` (신규) — 재료 → 메뉴 표. 실행 중에는 **이 표만** 본다.
  문자열 부분일치는 표를 만들 때 참고만 했다(양쪽으로 다 틀리기 때문).
  HARD(제외): 땅콩·견과류·새우·조개·오징어·낙지·문어·굴·생선회·마라·고수·곱창·순대·선지·내장
  SOFT(뒤로 밀기): 양파·마늘·파·버섯·청양고추 — 제외하면 남는 게 없다
- `lib/recommend.ts` — `getRecommendationsDetailed` 로 갈아엎음(옛 `getRecommendations` 는 껍데기로 남김)
  · 제외 → 가중(메뉴 x3/중분류 x2/대분류 x1) → 전원 선호 우선 → 씨앗 흔들림 → 빈 결과 되살림
  · 🔴 난수를 **비교 함수 안에서 쓰지 않는다.** 정렬 전에 후보별로 한 번 계산한다
- 모임 화면: 씨앗 = `모임id-YYYYMMDD`. 같은 날은 순서 고정, 날이 바뀌면 달라진다
- 홈 화면: 부분일치 필터 3곳을 `expandDislikes` 로 교체
- 확인: `node scripts/check-recommend.mjs` (9가지 경우 — 마라→마라탕 제외, 파→파스타 생존,
  전원 선호 우선, 같은 씨앗 같은 순서, 다 빼도 빈 화면 아님, score 없는 옛 행 동작)

### 재료 표를 DB 로 (2026-08-26, 코드는 끝 · 적용 대기)

- `supabase/migrations/20260826000003_menu_ingredients.sql` — 테이블 + 씨앗 151행 + 제보 RPC
- `src/lib/ingredientMap.ts` — DB 표를 받아 코드 표 위에 **더한다**(캐시 1회).
  🔴 실패하면 코드 표로 돈다. 못 먹는 음식을 거르는 일이라 "조용히 아무것도 안 걸러짐" 이 최악이다.
- 제보는 `report_menu_ingredient` RPC. `confirmed=false` 로 시작하고 **3명**이 모이면 반영된다
  — 아무나 확정시킬 수 있으면 장난으로 남의 메뉴를 지울 수 있다.
- 남은 일: 제보를 넣는 화면(2단계 스와이프에 같이), 관리자 검수 화면

### Vercel/Supabase 접근에 대해 알아낸 것 (2026-08-26)

- 이 환경에서 **Vercel CLI 는 인증돼 있고** 프로젝트가 링크돼 있다(`dalgonacrunchs-projects/meogja`).
  `npx vercel env ls`, `npx vercel env pull` 이 된다.
- 🔴 그런데 **Sensitive 로 등록된 값은 되읽을 수 없다.** `POSTGRES_URL`·`POSTGRES_PASSWORD`·
  `SUPABASE_SERVICE_ROLE_KEY` 는 pull 하면 값 자리에 `[SENSITIVE]` 문자열이 온다.
  → 그 경로로 DB 에 붙는 것은 불가능하다. 읽히는 것은 host/user/database/anon key 정도.
- 그래서 마이그레이션 적용에는 **Supabase 액세스 토큰(sbp_…)** 이나 DB 비밀번호가 필요하다.
  `npx supabase db push --db-url <url>` 또는 `link` 후 `--linked`.

✅ **마이그레이션 3개 실서비스 적용 완료 (2026-08-26)** — owner 가 준 액세스 토큰으로 push.
확인: `menu_ingredients` 151행(마라 4행), `score is null` 0건(양쪽 테이블), score/kind 컬럼 존재,
`increment_food_score`·`report_menu_ingredient` 둘 다 `auth.uid()` 확인 있음.

🔴 **db push 가 막혔던 이유와 해결** — 원격 이력에는 있는데 저장소에는 없는 마이그레이션이
4개 있었다(`20260612000001`, `20260614000001`, `20260617000001`, `20260618000001` — 다른
컴퓨터나 대시보드에서 적용한 듯). CLI 는 이 경우 push 를 거부한다.
→ **빈 자리 채우기 파일**(`*_remote_only_placeholder.sql`)을 만들어 이력을 맞췄다.
내용은 비어 있어야 한다(원격에는 이미 적용됨으로 기록돼 있어 다시 실행되지 않는다).
⚠️ 그 파일들을 지우면 push 가 다시 막힌다. CLI 가 권하는 `migration repair --status reverted`
는 **쓰지 마라** — 실제로 적용된 것을 되돌린 것으로 기록해 이력이 어긋난다.

풀 항목:
- [ ] 못 먹는 음식(알레르기·비선호)이 실제로 결과에서 **배제되는지** 확인 → 안 되면 하드 필터로
- [ ] 모임원 **공통 선호** 추출 로직(교집합 우선, 없으면 가중 합) 점검
- [ ] 같은 입력에 늘 같은 답이 나오는 문제 → 가중치에 소량 난수(같은 세션에서는 흔들리지 않게 시드 고정)
- [ ] 선호도 조사를 **최초 로그인 튜토리얼**로 (한 장씩 넘기는 게임 형태, 이탈 지점에서 저장)
- [ ] 조사 자체를 재미있게 — 월드컵/스와이프 등 이미 있는 놀이 요소 재사용

### 먼저 읽어 둘 진단 (2026-08-26 코드 확인, 아직 안 고침)

`lib/recommend.ts:getRecommendations` 를 읽어 확인한 것:

1. 🔴 **못 먹는 음식이 이름이 정확히 같을 때만 걸러진다.** `dislikes.has(메뉴명)` 식의
   완전일치 비교다. 그런데 프로필의 '재료/알레르기' 프리셋은 **재료 이름**을 그대로
   저장한다(`profile/page.tsx` 의 `INGR_PRESETS`: 고수·땅콩·새우·마라·양파·곱창 …).
   → `마라` 를 못 먹는다고 해도 **마라탕은 그대로 추천된다.** `새우` 도 새우튀김을
   못 막는다. owner 가 "제대로 되는지 모르겠다" 고 한 게 이 자리일 가능성이 크다.
   고치려면 재료 → 메뉴 포함관계(또는 메뉴별 재료 태그)가 필요하다.
2. 점수가 **좋아한 사람 수** 하나뿐이다(메뉴·중분류·대분류 좋아요를 모두 1점으로 셈).
   구체적인 취향(메뉴 지정)과 뭉뚱그린 취향(대분류)이 같은 무게다.
3. 랜덤은 **동점일 때만** 섞인다(`b.score - a.score || Math.random() - 0.5`).
   선호가 쌓이면 1등이 고정된다 → owner 가 말한 "너무 같은 결과만" 이 여기서 온다.
   반대로 선호가 거의 없으면 전부 0점이라 **완전 무작위**가 된다.

⚠️ 경쟁사 조사 결과를 보고 방향 정한 뒤 착수하기로 했다(랭킹·반경 방향이 바뀔 수 있어서).

---

## 다음에 할 일 (계속)

원래 있던 것:
- [x] eslint error(effect 안에서 setState) 2건 정리 (2026-08-26) — 모달을 열 때
      `openFindGroup` 이 로딩을 켜도록 바꿨다(effect 안 동기 setState 제거). 두 화면 error 0
- [ ] SPEC.md 의 미구현 항목들(AI 추천, 리뷰 UI, OG 이미지 공유 등)

---

*마지막 업데이트: 2026-08-26 (선호도 개편 1~3단계 — 재료 제외·점수 4단계·취향 스와이프·행동 학습 / 마이그레이션 3개 실서비스 적용)*
