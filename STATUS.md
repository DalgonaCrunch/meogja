# 현재 상태 — 세션 인수인계 (2026-08-26)

새 세션은 이 파일부터 읽는다. 그다음 `CLAUDE.md`(프로젝트 지침), `SPEC.md`(기능 명세).

- **역할 구분**: CLAUDE.md = 규칙 / **STATUS.md = 지금 어디까지 왔나(이 문서)** / SPEC.md = 무엇이 구현돼 있나
- 배포: `vercel --prod` 또는 **main 푸시 → Vercel 자동 빌드**(GitHub 연동돼 있음)
- 실서비스: https://meogja.vercel.app

---

## 이번 작업 (2026-08-26) — 주변 맛집 지도 보기

owner 요청: "주변식당 찾고 나서 결과를 맵으로 해서 현재위치랑 주변에서 어디에 있는지 보여줄 수 있어?"

### 만든 것
- `src/components/NearbyMap.tsx` (신규) — 카카오 지도 JS SDK 래퍼
- `src/app/nearby/page.tsx` — 정렬 탭 옆에 **목록 / 지도** 토글, 선택한 가게 카드
- `scripts/check-nearby-map.mjs` (신규) — 헤드리스 확인 스크립트

동작: 내 위치는 파란 점(pulse), 식당은 카테고리 이모지 마커. 마커를 누르면 그 마커가
주황으로 커지고 지도 아래에 가게 카드가 붙는다(카카오맵·네이버맵·구글맵 링크 +
"같이 먹을 사람 구하기"). 전체가 한 화면에 들어오도록 `setBounds` 로 줌을 맞춘다.

**데이터는 새로 받은 것이 없다.** `/api/nearby` 가 이미 카카오 FD6 검색의
`mapx`(경도)·`mapy`(위도)를 내려주고 있었고, 화면도 기준 좌표를 `coords` 로 들고 있었다.

### 커밋
| 커밋 | 내용 |
|------|------|
| `5edae4e` | 지도 기능 본체 (NearbyMap + 토글 + 카드) |
| `80440a1` | 마커를 고를 때마다 화면이 원위치로 튕기던 것 |
| `29fed19` | 실물 확인에서 드러난 셋 — 깨진 아이콘·잘린 이름표·안 보이는 카드 |

### 곁다리로 고친 원래 버그
`nearby/page.tsx` 의 `FOOD_ICONS` 가 `/food-icons/korean.png` 같은 **없는 영문 경로**를
가리키고 있었다. 실제 아이콘 140개는 전부 한글 파일명이고 매핑은 `lib/foodIcons.ts` 의
`getFoodIconUrl` 이 이미 갖고 있었다. 사진 API 가 빈손일 때마다 **목록 카드도 같이
깨지고 있었다** — 지도 카드를 만들다 발견해 양쪽을 함께 고쳤다(404 6건 소멸).

### 확인
- `npx next build` 통과 / `tsc --noEmit` 통과 / eslint 신규 error 0
- 로컬 헤드리스 통과, **실서비스 통과**(SDK 로드·타일·마커 탭 → 카드, 콘솔 에러 0)
- 배포본 번들에 JS 키가 실제로 박힌 것까지 파일로 확인

---

## 이어서 (2026-08-26) — 지도를 기본으로, 홈 진입 화면에도 지도

owner 요청: "지도를 기본값으로. 그리고 주변찾기 탭 말고 홈에서 주변식당찾기 눌러 들어갔을
때는 지도가 안 뜨는데 그때도 뜨게."

- `/nearby` 기본 뷰: 목록 → **지도**
- 홈의 "📍 모임 없이 바로 주변 찾기" 는 `/search` 로 간다 — **이 화면엔 지도가 아예 없었다.**
  같은 토글 + 지도를 붙였다(기본도 지도).
- 지도 + 가게 카드를 `src/components/MapPanel.tsx` 로 뽑았다. 카테고리·거리·지도링크
  헬퍼는 `src/lib/foodCategory.ts` 로 모았다 — 카드 복사본이 갈라져 아이콘 버그를 한쪽만
  고치는 일이 있었다(어제 그 일이 실제로 있었다).
- 🔴 **제공자별 좌표 형식이 다르다.** 네이버(`/api/search`)는 mapx/mapy 를 **1e7 배 정수**로
  내려준다(그 라우트는 거리 계산에만 나눠 쓰고 응답은 원본을 넘긴다). 카카오·구글은 십진 도.
  안 맞추면 마커가 지구 밖으로 간다 → `normalizeCoord` 가 흡수. 좌표 없는 결과는 지도에서
  빼고 문구로 알린다.
- 지도가 안 뜨면(키 없음·도메인 미등록·SDK 실패) `NearbyMap` 이 `onUnavailable` 로 알리고
  화면이 **자동으로 한 번 목록으로 되돌아간다.** 지도가 기본이 된 순간 이 대비가 없으면
  실패 시 결과가 통째로 안 보인다. 사용자가 다시 '지도'를 누르면 그대로 둔다.

확인: `tsc --noEmit` 통과 / `next build` 통과 / eslint 신규 error 0(기존 2건만 남음)
헤드리스: `node scripts/check-nearby-map.mjs`, `node scripts/check-search-map.mjs` 둘 다 통과
  - ⚠️ **포트 3000 으로 확인해야 한다.** 카카오 콘솔에 등록된 로컬 도메인이 `localhost:3000`
    뿐이다. 3311 등 다른 포트로 띄우면 SDK 가 401 을 주고 '지도를 불러오지 못했어요' 가 뜬다.
  - `/nearby` 목록 카드는 주소를 적지 않는다 — 목록 전환 판정에 주소 문구를 쓰면 안 된다.

알아두면 좋은 것: `/search` 는 `meogja_preset_menus` (고른 메뉴)가 없으면 검색을 아예 안 한다.
홈에서 메뉴를 안 고르고 들어오는 경로가 있으면 결과가 0건이라 지도도 비어 있다 — 이건 지도
문제가 아니다.

---

## 운영 설정 — 지도가 뜨려면 둘 다 필요하다

한쪽만 있으면 지도는 안 뜬다(앱은 안 깨진다. 안내 화면이 대신 뜨고 목록은 그대로 쓴다).

1. **카카오 콘솔의 JavaScript SDK 도메인** — 등록 완료
   `developers.kakao.com` → 내 애플리케이션 → 오늘뭐먹지?(meogja) → 앱 설정 > 앱 >
   **플랫폼 키** → JavaScript 키 [Default JS Key] 수정 → **JavaScript SDK 도메인**
   - 등록된 값: `https://meogja.vercel.app`, `http://localhost:3000`
   - ⚠️ 콘솔이 개편돼 '플랫폼' 메뉴가 없다. '일반' 페이지에도 없다. **플랫폼 키** 안이다.
   - 미등록이면 SDK 가 401 `domain mismatched` 를 준다. 이 한 줄로 원인이 바로 갈린다:
     `curl -s -H "Referer: https://meogja.vercel.app/" "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<JS키>&autoload=false" | head -c 120`
   - 새 도메인(커스텀 도메인 등)을 붙이면 여기에도 반드시 추가해야 한다.

2. **Vercel 환경변수 `NEXT_PUBLIC_KAKAO_JS_KEY`** — 등록 완료
   - ⚠️ **빌드 시점에 앱 안으로 박히는 값이다.** 값을 바꾸면 **재배포해야** 반영된다.
   - Secret 으로 등록돼 있어도 빌드는 읽는다(현재 그렇게 돼 있다). 다만 값을 다시 볼 수
     없다 — `NEXT_PUBLIC_` 값은 어차피 브라우저에 공개되므로 Config 가 더 편하다.
   - 배포본에 제대로 박혔는지 확인하는 법: 실서비스 JS 번들을 받아 키 문자열을 찾는다.

### 돈 드는 설정은 필요 없다
- 카카오맵은 **제품 설정 > 카카오맵 → 사용 설정 ON** 이면 되고, 이 앱에는 **무료 쿼터**가
  붙어 있다(앱 이름 옆 '카카오맵 무료 쿼터' 뱃지).
- **유료 API 화면의 '카카오맵' 은 '사용 안 함' 으로 두어야 한다.** '사용 함' 으로 바꾸면
  그때부터 사용 금액이 청구된다. 지도 표시에는 필요 없다.
- JS 키 수정 화면의 '카카오 로그인 리다이렉트 URI' 는 **비워 둔다.** 먹자냥의 카카오
  로그인은 Supabase 를 거치고, 그 주소는 제품 설정 > 카카오 로그인 쪽(REST 키)에 이미
  등록돼 있다. 여기에 넣으면 설정이 두 군데로 갈라진다.

---

## 로컬 개발에서 알아둘 것

- **로컬에는 실제 키가 없다.** Supabase·카카오 REST 키는 Vercel 환경변수에만 있어서
  `npm run dev` 로는 검색 API 가 500 을 낸다. 화면 확인은 `scripts/check-nearby-map.mjs`
  처럼 API 응답을 가짜로 끼워 넣어서 한다.
- 🔴 **dev 서버를 띄운 채 `next build` 를 돌리지 마라.** 빌드가 같은 `.next` 를 덮어써서
  돌던 dev 서버가 깨진다(증상: 멀쩡하던 화면이 갑자기 API 500/ERR_FAILED). 빌드는 dev 를
  멈추고 하거나, 빌드 뒤 dev 를 다시 띄운다.
- 🔴 **Playwright 로 이 앱을 확인할 때는 `serviceWorkers: "block"` 을 켜라.** PWA 라
  서비스워커가 요청을 가로채면 `page.route` 모킹이 통째로 무시된다(같은 스크립트가
  어떤 실행에서는 되고 어떤 실행에서는 안 되는 형태로 나타난다). 모킹은 `page.route`
  보다 `context.route` 가 안전하다.

---

## 텔레그램 봇 (이 저장소 밖)

- 실행: 저장소 밖 `/home/user/dev/tmp/run_telegrambot.sh meogja`
- meogja 전용 봇 토큰은 `meogja/.token` (권한 600, `.gitignore` 44행으로 커밋 제외)
- 봇: `@desktop_empty_1_bot`
- 프로젝트마다 토큰·STATE_DIR·tmux 세션 이름·작업 폴더가 갈려 있어 다른 봇을 죽이지 않는다.
- ⚠️ 런처 스크립트 자체는 `/home/user/dev/tmp` 에 있고 **그 폴더는 git 저장소가 아니다.**
  버전관리 밖이라 백업본(`run_telegrambot.sh.bak-*`)으로만 이력이 남는다.

### 시스템 프롬프트는 이제 프로젝트별로 렌더된다 (2026-08-26)

`system_prompt.txt` 는 **템플릿**이고, 런처가 `{{PROJECT}} {{TOKEN_FILE}} {{STATE_DIR}}
{{BOT_USERNAME}}` 를 채워 `$STATE_DIR/system_prompt.rendered.txt` 로 렌더한 뒤 claude 에
넘긴다(토큰 **값**은 렌더하지 않는다 — 경로만).

고친 문제: 공용 프롬프트에 tmp 봇의 토큰 경로와 chat_id 가 하드코딩돼 있어서, meogja 봇
세션이 그 지시대로 curl 답장을 하면 **엉뚱한 봇(`@dev183_tmp_bot`) 창으로 갔다.**

같이 바로잡은 것 — 🔴 **`getUpdates` 를 호출하지 않는다.** 토큰당 폴러가 1개뿐이라
채널 서버가 쓰는 자리를 빼앗아 **내 채널이 끊기거나 사용자 메시지를 잃는다.** 사용자
답장은 자동으로 `<channel>` 메시지로 들어오므로 질문을 보낸 뒤 턴을 끝내면 된다.
답장은 MCP reply 툴이 기본, curl 은 `sendMessage` 전용 예비 수단(이건 폴러와 무충돌).

---

## 다음에 할 일

지도 관련(작은 것부터):
- [ ] 마커가 겹칠 때 뭉치기(클러스터) — 지금은 15개라 견디지만 반경을 넓히면 이름표가 엉킨다
- [ ] "이 지역에서 다시 찾기" — 지도를 밀어서 옮긴 위치로 재검색(지금은 헤더에서만 위치 변경)
- [ ] 목록 ↔ 지도 전환 시 보고 있던 가게 유지(지금은 전환하면 선택이 풀린다)
- [ ] 아이콘 매핑 구멍: `술집`, `패스트푸드` 는 `getFoodIconUrl` 이 못 찾아 마스코트로 떨어진다

원래 있던 것:
- [ ] `nearby/page.tsx:100` eslint error(effect 안에서 setState) — 이번 작업 전부터 있던 것
- [ ] SPEC.md 의 미구현 항목들(AI 추천, 리뷰 UI, OG 이미지 공유 등)

---

*마지막 업데이트: 2026-08-26 (주변 맛집 지도 보기 — 카카오 지도 SDK, 실서비스 확인 완료)*
