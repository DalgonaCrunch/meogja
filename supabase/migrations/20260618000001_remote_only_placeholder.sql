-- 자리 채우기 파일 (2026-08-26 추가)
--
-- 이 버전(20260618000001)은 **원격 DB 에는 이미 적용돼 있는데 저장소에는 파일이 없다.**
-- 다른 곳(대시보드 또는 다른 컴퓨터)에서 적용한 것으로 보인다.
--
-- supabase CLI 는 원격 이력에 있는 버전이 로컬에 없으면 db push 를 거부한다.
-- 이력을 맞추려고 빈 파일을 둔다. **내용은 비어 있어야 한다** — 원격에는 이미
-- 그 버전이 적용됨으로 기록돼 있어 다시 실행되지 않는다.
--
-- ⚠️ 이 파일을 지우면 db push 가 다시 막힌다. 실제 내용이 필요하면
--    supabase db pull 로 원격 스키마를 받아 확인해야 한다.
